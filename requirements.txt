# --- IMPORTANT: version-pinned to match the trained pipeline (Python 3.11/3.12) ---
# paddle 3.x GPU wheels are NOT on PyPI. Install from paddle's index, matching CUDA:
#   CUDA 12.x: pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   CUDA 11.8: pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
#   CPU only : pip install paddlepaddle==3.3.1
# (so paddlepaddle-gpu is intentionally NOT listed below — install it separately as above)
paddleocr==3.4.1
paddlex==3.4.3
ultralytics==8.4.104
# torch must match nccl 2.25.1 that paddle pulls in. Proven combo: torch 2.7.1+cu126.
# NEWER torch (e.g. 2.13) needs a newer nccl symbol (ncclCommResume) and crashes.
# Install from pytorch index (NOT plain PyPI):
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu126
opencv-python-headless
Pillow
numpy
PyYAML
scikit-image
